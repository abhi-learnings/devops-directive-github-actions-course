name: HelloWorld_GitHub_Action

on:
 workflow_dispatch:

jobs:
 hello-world-job:
  runs-on: ubuntu-latest
  steps:
  #  - name: checkout repo
  #    uses: actions/checkout@v4
   - run: echo "Hello World"
     